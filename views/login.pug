extends home1

block content
    // This is an empty line
    // This is another empty line
    h2(style="text-align: center;font-weight: bold; margin-top: 40px;") You have successfully log in. Lets create some posts!
